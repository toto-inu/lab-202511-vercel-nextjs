{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Assignee": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "todos": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Todo"
          }
        }
      },
      "required": [
        "name",
        "email",
        "updatedAt"
      ]
    },
    "Todo": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "completed": {
          "type": "boolean",
          "default": false
        },
        "assignee": {
          "anyOf": [
            {
              "$ref": "#/definitions/Assignee"
            },
            {
              "type": "null"
            }
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "title",
        "updatedAt"
      ]
    }
  },
  "type": "object",
  "properties": {
    "assignee": {
      "$ref": "#/definitions/Assignee"
    },
    "todo": {
      "$ref": "#/definitions/Todo"
    }
  }
}