# 移行計画（サマリ版）

## 📅 実装スケジュール（4-6週間）

### Phase 1: データモデル構築（1週間）
```bash
# 1. Prismaスキーマ拡張
# 追加: Plan, Tenant, TenantMember, Project, ProjectMember
# 拡張: User(isGlobalAdmin追加), Todo(tenantId, projectId追加)

# 2. マイグレーション
cd app
bunx prisma migrate dev --name add_multitenancy

# 3. Seedデータ作成
bunx prisma db seed
# → Plan 3件（FREE/PRO/ENTERPRISE）
# → default Tenant作成
# → default Project作成
# → 既存TodoにtenantId/projectId設定

# 4. 既存User移行
bunx tsx scripts/migrate-users.ts
# ADMIN → default TenantのOWNER
# USER → default TenantのMEMBER

# 5. User.roleカラム削除
bunx prisma migrate dev --name remove_user_role_enum
```

### Phase 2: 認証・認可拡張（1週間）
```bash
# Better Authセットアップ
bun add better-auth

# lib/auth.ts             - Better Auth設定
# lib/auth-utils.ts       - getCurrentUser, getCurrentTenant
# lib/auth/permission.ts  - requireAuth, requireTenantRole, requireProjectRole
# lib/plan-utils.ts       - checkResourceLimit, hasFeature
# middleware.ts           - セッションチェック
```

### Phase 3: コア機能実装（1-2週間）
```bash
# Server Actions
actions/project.ts  - getProjects, createProject
actions/tenant.ts   - switchTenant, getUserTenants
actions/todo.ts     - 権限チェック追加

# UIコンポーネント
components/TenantSwitcher.tsx
components/ProjectCard.tsx
components/ResourceLimitBadge.tsx

# ページ
app/projects/page.tsx
app/projects/[id]/page.tsx
app/settings/tenant/page.tsx
```

### Phase 4: 管理画面（1週間）
```typescript
// app/app/admin/[[...nextadmin]]/route.ts
// グローバル管理者のみアクセス可能
// Tenant/Plan/User管理
```

### Phase 5: 既存機能移行（1週間）
```bash
# ルーティング変更
/ → /projects （リダイレクト）
/projects/[id] → Todo一覧

# 既存データ確認
# Todo UI更新（priority, dueDate追加）
```

## ✅ 各Phase成功条件

| Phase | チェック項目 |
|-------|------------|
| 1 | マイグレーション成功、既存データ保持 |
| 2 | Better Auth認証動作、初回ログイン時Tenant作成 |
| 3 | プロジェクト一覧表示、Tenant切り替え動作、権限制御 |
| 4 | `/admin`アクセス、Tenant/Plan管理可能 |
| 5 | 全Todo操作正常、既存データ表示 |

## 🔄 重要な移行スクリプト

### 既存User → TenantMember
```typescript
// scripts/migrate-users.ts
const users = await prisma.user.findMany();
for (const user of users) {
  // 既存roleに基づいてTenantRole決定
  const tenantRole = user.role === 'ADMIN' ? 'OWNER' : 'MEMBER';

  await prisma.tenantMember.create({
    data: {
      userId: user.id,
      tenantId: defaultTenant.id,
      role: tenantRole
    }
  });
}
```

## 🔙 ロールバック手順

```bash
# Phase 1
bunx prisma migrate reset

# Phase 2-5
git checkout main
bun install
bunx prisma migrate deploy
```

## 📊 データバックアップ

```bash
# 各Phase開始前
docker exec to-saas-db-1 pg_dump -U postgres todo_db > backup-phase-X.sql
```

## 🧪 テストチェックリスト

- [ ] マルチTenant: 異なるTenant間でデータ漏洩なし
- [ ] 権限: Role別に適切な操作制限
- [ ] Plan制限: 上限到達時に適切にエラー
- [ ] Tenant切り替え: 1秒以内に完了
- [ ] パフォーマンス: 10,000 Todo でも2秒以内にロード
